---
title: "Release Status"
description: "Understand the stable release, development tree, and pinned FRP version."
---

# Release Status

FRP Auto Deploy separates the immutable stable release from the mutable `main` branch.

## Current state

| Item | Current |
| --- | --- |
| Published stable release | **v2.2.1** |
| Project version | **2.2.1** |
| Pinned / tested FRP | **v0.71.0** |
| Stable install source | immutable `v2.2.1` tag |
| Release commit | `19d4b6fb8a9bee2d477ace6f5c3ed70310e7ea8f` |
| Qualified candidate | `2140be5b6342c3651c16a458f8ea1bc9b577d992` |
| Full Real E2E | **PASS / PASS on the same exact candidate HEAD** |
| Product repository `main` | `c7fe407d4dab12896469ef29cb7a882ad6311bd0` after PR #14 housekeeping closure |

The qualified candidate and the v2.2.1 release tree were identical. Published v2.2.0 remains an immutable historical release and was not retagged.

## Canonical documentation closure

The post-release documentation restoration phase **`CANONICAL_PRODUCT_MASTER_RESTORE_V2_2_1_DOCS_CLOSURE`** finished with **PASS**.

| Item | Result |
| --- | --- |
| Final repository `main` HEAD after docs closure | `343f292d91d01c3f263ff5c443f101d185b5527f` |
| v2.2.1 release commit | `19d4b6fb8a9bee2d477ace6f5c3ed70310e7ea8f` |
| v2.2.1 tag target changed | **NO** |
| v2.2.0 tag changed | **NO** |
| New release created | **NO** |
| Product runtime files changed | **NO** |
| Real E2E rerun required | **NO** |
| Canonical Product Master restore | **PASS** |
| Product Master structural check | **PASS** |
| Current release/platform matrix updated | **YES** |
| Release history preserved | **YES** |
| Provenance added | **YES** |
| Known corruption removed | **YES** |
| Public metadata scan | **PASS** |
| Secret scan | **PASS** |
| `SHA256SUMS` updated | **YES** |
| Final documentation state | **CANONICAL** |

The damaged/truncated 2,173-line derivative was replaced by the Owner-reconstructed full integrated Product Master (approximately 3,625 lines) through **PR #12**. The restored authoritative document is `docs/PRODUCT_MASTER.md`. The incomplete surgical PR #13 was superseded and closed.

The Product Master documentation-only merge advanced product `main` from the v2.2.1 release commit to `343f292…` without changing the immutable v2.2.1 tag or release payload. A later README/CI housekeeping closure, PR #14, advanced product `main` again to `c7fe407…`. The **stable install identity remains v2.2.1 / `19d4b6f…`** throughout.

## PR #14 CI and repository housekeeping closure

The follow-up phase `PR14_CI_AND_REPO_HOUSEKEEPING_CLOSURE` also finished with **PASS**.

| Item | Result |
| --- | --- |
| Final product repository `main` | `c7fe407d4dab12896469ef29cb7a882ad6311bd0` |
| PR #14 | **MERGED** |
| Required checks | **PASS** — lint + all 7 distro jobs |
| macOS workflow | **PASS** after correcting docs/dist parity; no client-port product regression |
| Windows workflow | **PASS** |
| Product runtime files changed | **NO** |
| Full Real E2E rerun | **NO** |
| New release/tag change | **NO** |
| Old PR #1 / #2 | **CLOSED as superseded** |
| Final repository state | **CLEAN** |
| Owner action / manual E2E required | **NO** |

<Note>
  The earlier `docs/GITHUB_SETUP.md` follow-up was based on a stale path. The actual file is root `GITHUB_SETUP.md`, which was already current for v2.2.1; `docs/GITHUB_SETUP.md` does not exist. Final result: **`GITHUB_SETUP_STALE_STATE_NOT_APPLICABLE=YES`**.
</Note>

## Use stable for field installs

For a normal installation, use the immutable v2.2.1 release tag:

```bash
curl -fsSL \
  https://raw.githubusercontent.com/datarelay-labs/frp-auto-deploy/v2.2.1/dist/bootstrap-server.sh \
  | sudo bash
```

Do not replace the immutable tag with mutable `main` unless you intentionally want development-channel behavior.

## FRP version policy

The project does not automatically install the newest upstream FRP. The upstream binary is pinned, tested, and validated before adoption. `show upstream` is informational.

Current pinned FRP:

```text
0.71.0
```

## Stable platform status

v2.2.1 includes Real E2E validation for **Ubuntu 24 physical, Rocky Linux 8.10, Rocky Linux 9.4, Amazon Linux 2023, macOS Apple Silicon, and Windows 10 / PowerShell 5.1**.

Amazon Linux 2 remains **container/CI portability only**, and PowerShell 7 is **CI validated** unless a real test host actually has `pwsh` installed. See [Supported Platforms](/reference/platforms) for the exact validation matrix.
